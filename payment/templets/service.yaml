apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: roboshop
spec:
  type: ClusterIP
  selector:
      name: roboshop
      env: {{.Values.service.env}}
      running: payment
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080