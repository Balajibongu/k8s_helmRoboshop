apiVersion: v1
kind: Service

metadata:
  name: mongodb-headless
  namespace: roboshop

spec:
  clusterIP: None

  selector:
    name: roboshop
    env: dev
    running: mongodb

  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017